<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/images/logoDisplay.png" />
    <link rel="stylesheet" href="/css/update.css" />
    <title>SkillConnect - Upload Image</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        text-align: center;
      }
      body {
        width: 100%;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #f5f5f5;
      }
      div {
        text-align: center;
        height: 50%;
        width: 60%;
        border: 2px solid black;
        display: flex;
        flex-direction: column;
        justify-content: space-evenly;
        align-items: center;
        border-radius: 1rem;
        background-color: #3d405b;
        box-shadow: 0 0 10px white;
        padding: 1rem;
      }
      input {
        background-color: rgb(255, 255, 255);
        padding-left: 1rem;
        border-radius: 1.5rem;
        height: 3rem;
        width: 25em;
        font-size: 1.5rem;
        color: black;
        border: 2px solid black;
        text-align: center;
      }
      h1 {
        color: white;
        font-size: 3rem;
      }
      form {
        display: flex;
        flex-direction: column;
        justify-content: space-evenly;
        align-items: center;
      }
      button {
        width: 10rem;
        height: 3rem;
        font-size: 1.5rem;
        border-radius: 1rem;
        background-color: yellow;
      }
      button:hover {
        background-color: green;
        color: white;
      }
    </style>
  </head>
  <body>
    <div>
      <h1>Edit Profile Image</h1>
      <form
        method="POST"
        action="/home/<%=role%>/profile/<%=id%>/image"
        enctype="multipart/form-data"
      >
        <input
          type="file"
          placeholder="Upload New Profile Image"
          accept="image/*"
          name="image"
        />
        <br /><br /><br />
        <button>Submit</button>
      </form>
    </div>
  </body>
</html>
